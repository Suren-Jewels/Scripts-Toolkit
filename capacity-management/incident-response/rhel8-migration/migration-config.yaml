Scripts-Toolkit/capacity-management/incident-response/rhel8-migration/
    -> migration-config.yaml
    ->
migration:
  batch_size: 25
  canary_percentage: 10
  rollback_on_blocker: true
  notify_channels:
    - slack
    - pagerduty
    - opsgenie

paths:
  hostfile: "hosts.txt"
  history_dir: "migration-history"
  anomaly_dir: "migration-anomalies"
  metrics_dir: "migration-metrics"
